export const handler = async (event) => {
  // TODO implement
  const response = {
    statusCode: 200,
    body: {
      message: "Proceso terminado existosamente :)",
    },
  };
  return response;
};
